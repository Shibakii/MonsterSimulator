<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meaburro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeAburro">
        <activity android:name=".ShopFragment"></activity>
        <activity
            android:name=".PopUpMuerte"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MeAburro.Death" />
        <activity android:name=".MainGame" />
        <activity
            android:name=".PopUpNombre"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MeAburro.PopUp" />
        <activity
            android:name=".PickCharacter"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainMenu"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>